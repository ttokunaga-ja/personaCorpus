export type BetaCursor={createdAt:string;betaReleaseId:string};export const encodeCursor=(v:BetaCursor)=>Buffer.from(JSON.stringify(v)).toString("base64url");export function decodeCursor(value:string):BetaCursor{const v=JSON.parse(Buffer.from(value,"base64url").toString());if(typeof v.createdAt!=="string"||typeof v.betaReleaseId!=="string")throw new Error("invalid_cursor");return v;}export const compareBeta=(a:BetaCursor,b:BetaCursor)=>a.createdAt.localeCompare(b.createdAt)||a.betaReleaseId.localeCompare(b.betaReleaseId);

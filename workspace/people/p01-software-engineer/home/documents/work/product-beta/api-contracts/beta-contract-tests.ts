import {validateCreate} from "./beta-validator";const valid={version:"beta-r7.0"};const legacyAlpha={version:"r7.0"};if(validateCreate(valid).length)throw new Error("valid preview rejected");if(!validateCreate(legacyAlpha).some(i=>i.path==="$.version"))throw new Error("alpha version must remain outside beta preview");if(!validateCreate({version:"beta-r7.0",cancelAfterApproval:true}).some(i=>i.code==="enum"))throw new Error("gate rule missing");

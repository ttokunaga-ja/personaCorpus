openapi: 3.0.3
info:
  title: Product Alpha R7 release-gates API
  version: 7.2.0
  description: Synthetic Aster Loop contract API-A7-3812; reviewed on 2026-07-20.
paths:
  /v7/release-gates/{recordId}:
    get:
      operationId: register3812
      responses:
        '200':
          description: Reviewed synthetic contract response
          content:
            application/json:
              schema:
                type: object
                required: [contract_id, status]
                properties:
                  contract_id:
                    type: string
                    example: API-A7-3812
                  status:
                    type: string
                    example: reviewed

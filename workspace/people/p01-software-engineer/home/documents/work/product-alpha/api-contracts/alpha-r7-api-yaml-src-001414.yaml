openapi: 3.0.3
info:
  title: Product Alpha R7 event-deliveries API
  version: 7.4.0
  description: Synthetic Aster Loop contract API-A7-1414; reviewed on 2026-07-20.
paths:
  /v7/event-deliveries/{recordId}:
    get:
      operationId: publish1414
      responses:
        '200':
          description: Reviewed synthetic contract response
          content:
            application/json:
              schema:
                type: object
                required: [contract_id, status]
                properties:
                  contract_id:
                    type: string
                    example: API-A7-1414
                  status:
                    type: string
                    example: reviewed
